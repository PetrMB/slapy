/* ============ Hloubkové vrstevnice (izobaty) ============
   Zatím nevygenerováno — vytvoří je CI workflow „bake-terrain"
   (tools/bake_terrain.py) jako ORIENTAČNÍ MODELOVÝ ODHAD hloubek
   (skutečná batymetrie Povodí Vltavy není veřejná). */
const ISOBATHS = null;
const DEPTH_GRID = null;
